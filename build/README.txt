Index construction code lives in this directory.

Run the production builder from the project root:

    python -m build.build_production [arguments]

The runtime Web UI does not import this package.
